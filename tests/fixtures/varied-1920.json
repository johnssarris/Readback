{
  "kind": "render",
  "text": "sample-varied.txt",
  "note": "Chromium-rendered stand-in. Geometry metrics only - not valid for atlas comparison.",
  "truth": {
    "cellWidthPx": 11.1328125,
    "cellHeightPx": 23,
    "bodyTopY": 51,
    "bodyBottomY": 1010,
    "gutterRightEdgeX": 36,
    "textAreaLeftX": 36,
    "rowCount": 41,
    "lineCount": 40,
    "displayRows": [
      "#!/usr/bin/env python3",
      "\"\"\"Ledger reconciliation: 0O1lI are the characters that get misread.\"\"\"",
      "",
      "import re",
      "from collections import defaultdict",
      "",
      "PATTERN = re.compile(r\"^\\s*(?P<id>[A-Z]{2}-\\d{4})\\s+(?P<amt>-?\\d+\\.\\d{2})\\s*$\")",
      "LOOKALIKES = {\"0\": \"O\", \"O\": \"0\", \"1\": \"l\", \"l\": \"1\", \"I\": \"1\", \"|\": \"I\"}",
      "SCHEMA = {\"id\": \"AA-0000\", \"amount\": -0.01, \"flags\": [\"reconciled\", \"manual\", \"carried-over\"], \"note\": \"long enough to wrap even in a maximised window, which is the",
      "point of it\"}",
      "# punctuation sweep: ~!@#$%^&*()_+`-={}|[]\\:\";'<>?,./ and again ~!@#$%^&*()_+ so the row runs past the right edge of a 1920px window and has to wrap",
      "",
      "",
      "def parse(lines):",
      "    out = defaultdict(list)",
      "    for n, line in enumerate(lines, start=1):",
      "        if not line.strip():",
      "            continue",
      "        m = PATTERN.match(line)",
      "        if m is None:",
      "            if line.lstrip().startswith(\"#\"):",
      "                continue",
      "            raise ValueError(f\"line {n}: {line!r}\")",
      "        out[m[\"id\"]].append(float(m[\"amt\"]))",
      "    return out",
      "",
      "",
      "def reconcile(ledger, *, tolerance=0.01, verbose=False):",
      "    totals = {}",
      "    for key, amounts in sorted(ledger.items()):",
      "        if len(amounts) < 2:",
      "            totals[key] = amounts[0] if amounts else 0.0",
      "            continue",
      "        credit = sum(a for a in amounts if a > 0)",
      "        debit = sum(a for a in amounts if a < 0)",
      "        if abs(credit + debit) <= tolerance:",
      "            totals[key] = 0.0",
      "        else:",
      "            totals[key] = round(credit + debit, 2)",
      "            if verbose:",
      "                print(f\"  {key}: {credit:>10.2f} {debit:>10.2f} -> {totals[key]:>10.2f}\")"
    ],
    "rowYCenters": [
      62.5,
      85.5,
      108.5,
      131.5,
      154.5,
      177.5,
      200.5,
      223.5,
      246.5,
      269.5,
      292.5,
      315.5,
      338.5,
      361.5,
      384.5,
      407.5,
      430.5,
      453.5,
      476.5,
      499.5,
      522.5,
      545.5,
      568.5,
      591.5,
      614.5,
      637.5,
      660.5,
      683.5,
      706.5,
      729.5,
      752.5,
      775.5,
      798.5,
      821.5,
      844.5,
      867.5,
      890.5,
      913.5,
      936.5,
      959.5,
      982.5
    ],
    "fontPx": 19,
    "wrapped": true
  }
}
